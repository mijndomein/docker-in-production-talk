#!/usr/bin/env bash

cp demo/config/parameters.default.yml demo/config/parameters.yml

bin/composer install

docker-compose \
    -p demo \
    up -d --build docker-demo-api

docker-compose \
    -p demo \
    up --build docker-demo
